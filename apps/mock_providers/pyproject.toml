[project]
name = "mock-providers"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic-settings~=2.12.0",
    "cachetools~=7.0.1",
    "lib[fastapi]",
]

[tool.pytest.ini_options]
pythonpath = [".", "../liquidity_orchestrator"]
asyncio_mode = "auto"
asyncio_default_test_loop_scope = "session"

[tool.uv.sources]
lib = { workspace = true }
